# Changelog

版本規則：從「習慣 / 專案」等新想法開始的開發為 **1.x**；先前已發佈的基礎版為 **0.x**。
開發流程：本地開發與測試為主，版本管理手動進行，不再自動 commit / push GitHub / 部署。

## [1.13.0] — 計畫視圖重整：規劃為主、安排收逾期、習慣移出

### Changed
- **習慣移出「每日規劃」**：待辦頁不再顯示習慣(今日/漏做)，習慣只在「養成」頁管理。
  看板/清單持續排除習慣任務。
- **「安排」折疊面板**取代原「整理區」：收合僅標題(顯示逾期數)；展開＝新增待辦輸入 ＋ 逾期；
  逾期再展開＝細項。三層折疊。
- **逾期可拖進週看板**：在「本週」鏡頭，逾期每列有拖曳握把，直接拖到某天改期
  (頁面級 `DndContext`，`WeekBoard`→`WeekGrid` 共用同一拖曳環境)；同時保留「☀️今天／改期」點按。

### Removed
- 移除 `PlanSetup`、`HabitsRail`(習慣不再進每日規劃)。

## [1.12.0] — 週計畫：專案排程、計畫合併、常駐軌重整

### Added
- **週計畫拖曳板**（待辦新增「本週」鏡頭）：週一～週日、可跨天拖曳、每天底下快速加、往下週翻規劃未來，全程不用選日期。
- **專案就地排程**：專案步驟旁「📅 排程」選一天即自動變成該天待辦、自動進週計畫（`pushStepToTodo` / `moveTaskToDay`）。
- **逾期常駐軌**（`OverdueRail`）：過去未完成一次攤開，✓完成 / ☀️移到今天 / 改期 / 刪除——BuJo 遷移的化身。
- **今日習慣獨立軌**（`TodayHabitsRail`）：今日習慣抽出成一軌，不再混進日欄/清單。

### Changed
- **「週計畫」與「計畫」合併**為單一「計畫」：模式列變 計畫 / 紀錄；內用鏡頭切換 **本週看板 ↔ 全部清單**（預設本週）。
- **常駐軌重整**：逾期、今日習慣、習慣待辦（`HabitBacklogRail`）、安排待辦（`AddTaskBar`，緊湊、預設收合）兩個鏡頭都在、往上收攏。
- 看板/清單一律**排除習慣任務與逾期**（各自有專屬軌），不重複顯示。
- TodoLogPage 拆出 `WeekBoard`／`OverdueRail`／`TodayHabitsRail`／`HabitBacklogRail`／`AddTaskBar` 等元件。

## [1.11.0] — 首頁習慣總覽：目標缺口與過去漏做

### Changed
- **首頁習慣總覽逐一列出每個習慣**（不再只列今日該做）：每列顯示今日狀態（可打勾／已完成／休息日）、
  🔥連續天數、**目標進度 N/target 天與「還差 X 天」**（達標顯示「已達標 🎉」）。
- **過去未完成**：每個習慣顯示「過去漏 N 次」（過去已排定但未打勾的次數），底部小結「過去共漏 M 次」。

## [1.10.1] — 修正「累計獲得」計算

### Fixed
- **累計獲得**（`getTotalEarned`）改為「完成金額淨扣退款」（`task_complete` + `task_revoke`），
  排除願望兌換。先前只加正交易，導致完成後又取消完成／刪除時退款未被扣掉，反覆切換會虛高。
  撲滿餘額（`getCurrentBalance`）本來就正確，此次僅修累計數。

## [1.10.0] — 待辦大改版：跨日拖曳、編輯、緊急/任務、首頁習慣總覽

### Added
- **跨日拖曳改期**：計畫模式改為單一 `DndContext`（`PlanBoard`），把任務拖到別天的分組即改
  `scheduledDate`；同日內仍可排序。
- **編輯已建立的待辦**：任務卡「編輯」可改標題＋分類（手動任務；習慣/專案來源不開放）。
- **安排待辦直接新增標籤**：分類列尾端「＋ 標籤」即建即選（`addCategory` 回傳 `CategoryDef`）。
- **計畫模式時間篩選**：全部／今天／未來一週／自訂日期範圍。
- **緊急旗標**：任務卡🚩一鍵標記緊急（`Task.priority='high'`），浮到當天分組最前＋紅色標示。
- **一鍵設為「任務」**（參考飛書）：📌置頂到「任務」面板，顯示在待辦頁與首頁最上方；
  新增 `Task.pinned`、`toggleTaskPin`、`toggleTaskUrgent`。
- **首頁未來待辦**：`TodaySnapshot` 下方列出未來日期未完成的待辦，可提前完成。
- **首頁習慣總覽**：`HabitsOverview` 顯示今天習慣已完成/未完成數、連續天數，可直接打勾。

### Changed
- **記錄到未來日期＝待辦**：未來/今天新增為待辦（打勾才入帳）；只有過去日期才補記為完成。
- **勾選雙向切換**：已完成的待辦可再點取消完成並退款（`uncompleteTask`）。
- **安排待辦區塊可折疊**；折疊箭頭移到最右，與習慣待辦一致。
- **卡片操作鈕響應式**：緊急/任務/編輯/刪除在窄螢幕只顯示圖示、寬螢幕（`sm`）加文字。

## [1.9.0] — 待辦拖曳排序、移除優先級顯示

### Changed
- **待辦「計畫」模式可拖曳排序**：每筆左側拖曳握把（`@dnd-kit`，滑鼠/觸控皆可），
  拖動即調整順序並持久化（`Task.order`，同步雲端）；依日期分組各自排序，新任務排最後。
- **移除高/中/低優先級標籤**：`TaskItem` 不再顯示/切換優先級（改用拖曳決定順序）。
  `Task.priority` 欄位與 store action 仍保留，未來要用再接。

## [1.8.0] — 瀏覽器擴充（Chrome／Edge MV3）

### Added — 把完整 App 做成瀏覽器擴充
- **三種入口**：①工具列**彈窗**（400×600）②**常駐側邊欄**（`chrome.sidePanel`，彈窗右上「↗ 側邊欄」切換）
  ③**任何網頁上的浮動面板**（content script 注入邊緣小頁籤🐷，點開滑出，類似 BigGo）。
- **浮動面板**：`extension/content.js`（純 JS，publicDir 複製）在 `<all_urls>` 注入 shadow-DOM
  頁籤＋面板，面板用 iframe 載入擴充自己的 `panel.html`（auth/state 在擴充 origin，跨站一致）；
  `web_accessible_resources` 開放 panel.html/assets。極少數嚴格 CSP(frame-src) 網站可能擋 iframe。
- **重用現有 React 程式**：獨立打包設定 `vite.config.extension.ts` → `dist-extension/`，
  入口 `popup.html`／`sidepanel.html` 共用 `main.tsx`；不含 PWA service worker。
- `extension/manifest.json`（MV3，`action.default_popup` + `side_panel`，
  `host_permissions` 指向 Supabase），圖示由 `scripts/ext-icons.mjs` 生成（16/32/48/128）。
- **登入**：擴充內 Email 登入可用（同一 Supabase 帳號、同步資料）；Google 登入在擴充環境
  需 `chrome.identity` 額外設定，故在擴充內**暫時隱藏**，先用 Email。
- 建置：`npm run build:ext` → 到 `chrome://extensions` 開啟開發人員模式 → 載入未封裝 →
  選 `dist-extension/`。（擴充無法像網頁部署，需載入未封裝或上架 Chrome Web Store。）

## [1.7.0] — PWA + 督促提醒 + LINE 開網頁

### Added — 督促提醒機制（LINE 每日推播）
- **Vercel Cron 兩班**：08:00（早安立志）、21:00 台北（晚間收割）。`api/cron-nudge.ts`。
- **四種推播**：☀️早安（今天排了幾件）、🌙晚間（入帳/完成數＋未打勾）、
  🔥連續救援（streak≥3 今天沒打勾的習慣）、🎁願望臨門（主目標差 ≤20%）。
  晚間班把收割＋救援＋願望合成「一則」，避免轟炸。
- **LINE 內開關**：打「提醒」跳出設定卡片，4 種各自可開/關（存在 `settings.nudge`）。
- **無需新表**：訂閱者由 `user_data`（line userId 為 key）推導；prefs 與
  idempotency（lastMorning/lastEvening）都存在 `settings.nudge`，網頁端 `normalizeSettings`
  已保留不被覆蓋。手動測試：`/api/cron-nudge?key=tdbk-setup&slot=evening&dry=1`。

### Added — PWA（可安裝、像 App）
- `vite-plugin-pwa`：manifest（主色 #00804F）＋ service worker 預快取 app shell，
  可「加到主畫面」、離線開啟。圖示 192/512/maskable/apple-touch（`scripts/pwa-icons.mjs`）。
  雲端資料**不快取**以維持同步即時。

### Added — LINE 開啟網頁版
- 打「網頁」跳出帶連結卡片；撲滿卡片底部加「🖥 開啟網頁版」鈕；歡迎詞附網址。

## [1.6.1] — LINE 導覽用按鈕（手機+電腦通用）

### Changed — 不用 Rich Menu，改 Quick Reply 按鈕
- **導覽改成 Quick Reply 按鈕**（待辦｜撲滿｜願望｜養成｜新增），附在每則回覆上方。
  原因：Rich Menu 在**電腦版 LINE 不顯示**，按鈕則手機電腦都能用。已移除 Rich Menu。
- **待辦卡片底部**改為一行淺色提示「＋ 直接打字即可新增待辦」。
- **養成**：點進去跳出「習慣／專案」選擇，再看各自內容（專案改用進度卡）。
- 願望主目標（釘選）在網頁與 LINE 皆**置頂**。
- 一鍵設定/清除 Rich Menu 的部署端點：`/api/setup-richmenu?key=tdbk-setup`
  （`&action=clear` 清除、`&check=1` 檢查），免終端機、免複製密碼。

## [1.6.0] — 3 分頁整併 + 同步修復 + LINE 易讀化

### Changed — 用設計師視角把 7 分頁收斂
- **撲滿**：保留餘額/今日進度 + 最近願望卡（teaser）。
- **願望**：維持獨立分頁（清單、進度條、釘選、兌換）。
- **待辦**：併入「日記」——「紀錄」模式底部新增「今日記事」折疊區，離開輸入即存，
  若設定「寫日記算一件」則當日入帳一次。
- **養成（新）**：把少用的「習慣」「專案」收進同一分頁，上方切換。
- 底部導覽從 6 項變 **4 項（撲滿·待辦·願望·養成）**，設定維持右上角齒輪。
- 依使用者實際使用整併，未刪任何資料。

### Fixed — LINE ↔ 網頁同步
- 網頁分頁**重新取得焦點時自動重抓雲端**（throttle 1.5s），在 LINE 改的待辦/設定
  回到電腦即可見，不必手動重整；也降低舊分頁覆蓋雲端的風險。

### Changed — LINE 卡片更易懂
- **完成/未完成一眼分辨**：未完成的每筆做成區塊，含粗體標題 +「✅ 完成」按鈕；
  已完成集中在「── 已完成 ──」下方，灰色「✔ 標題 +NT$」。不再只用一個圖示。
- **大/小任務按鈕講人話**：小任務顯示「改大 +NT$30」、大任務顯示「改小 +NT$10」，
  動作一看就懂（取代原本看不懂的「小 NT$10」）。

## [1.5.0] — LINE Bot 介面化（Phase 1）

### Added — 在 LINE 裡用按鈕操作，不用跳網頁
- **Flex 卡片**：`待辦`／`撲滿`／`願望清單`／`習慣` 改用卡片呈現。
  待辦與習慣卡片每一項右側有 **✅ 一鍵完成**鈕（postback），完成即時入帳並回傳更新後的卡片；
  撲滿與願望卡片有進度條，願望可直接 **🎯 釘選 / 🎁 兌換**。
- **Quick Reply 快捷鈕**：每則回覆上方常駐 待辦／撲滿／願望／習慣／新增／說明 氣泡鈕。
- **Rich Menu 常駐選單**：聊天室底部 6 格選單（待辦·新增·習慣／撲滿·願望·說明）。
  圖檔 `brand/richmenu.png` 由 `npm run richmenu:image` 產生（一次性），
  `npm run richmenu:setup` 註冊到 bot 並設為預設（部署後執行一次）。
- **postback 路由 + follow 歡迎詞**：webhook 新增 `postback`／`follow` 事件處理；
  既有文字指令全部保留為備援。
- **回覆即卡片、零廢話**：移除所有「✅ 已新增…／🎉 完成…已入帳」文字確認泡泡；
  任何操作（新增／完成／兌換／釘選／刪除）一律回傳「更新後的卡片」當唯一回覆，
  少一則訊息、少雜訊，收穫感保留在卡片的今日 +NT$ 與 ✅ 狀態上。
  除了「打字新增待辦」必須傳訊息，其餘全靠按鈕。
- **免前綴新增 + 大/小用按鈕**：在 LINE 直接打字輸入事項即新增（不需 `新增`／`新增大` 前綴，
  仍保留為備援）；待辦卡片上每筆「金額」是可點切換鈕（`小 ⇄ 大`），
  決定任務大小從打字改成一指輕點，且加完後隨時可改。習慣／專案來源任務金額固定不顯示切換。

### Notes
- LINE 推播/按鈕無法本地測試，需部署到 Vercel 才能實機驗（已取得授權）。
- 新增 dev 依賴 `sharp`（僅用於產生 rich menu 圖，非執行期相依）。

## [1.4.0] — 本地開發（尚未部署）

### Changed — 待辦「計畫 / 紀錄」雙模式（依專家+自用者重評）
- 待辦頁上方切換 **計畫 / 紀錄** 兩種模式：
  - **計畫**（面向未來）：安排待辦到今天或未來日期（沿用大小/分類＝獎勵），
    依日期分組（逾期/今日/明天/未來），打勾完成即入帳；優先級保留在任務卡標籤。
  - **紀錄**（面向過去）：「補記剛完成的事」一句話即時入帳；完成時間軸＋每日入帳＋時間篩選。
- **移除拖曳優先級看板**（自用情境摩擦過高），保留三級優先級標籤。
- 背景：六位專家/用戶重評後定調——規劃與記錄是自用者剛需，該減的是「儀式重量」而非能力本身。

## [1.3.0] — 本地開發（尚未部署）

### Added
- **優先級拖曳排序**：優先級檢視改為可拖曳看板（`@dnd-kit`，支援手機長按拖曳）。
  在高/中/低區帶內拖曳可調整順序，跨區帶拖曳即改變優先級；以 `order` 欄位持久化。

## [1.2.0] — 本地開發（尚未部署）

### Added
- **待辦「優先級」檢視**：任務新增 `priority`（高/中/低）；待辦頁第三種顯示
  方式「優先級」，依高/中/低分組、未完成在前。
- **快速記錄**：優先級檢視頂部有快速新增列（選優先級 + 輸入即記今日，IME 友善）。
- **一鍵調整**：每張任務卡片有可點的優先級小標籤，點一下循環 高→中→低。
- 工作待辦＝用既有「工作」分類在此檢視篩選（未另開 Tab）。

## [1.1.0] — 本地開發（尚未部署）

### Added
- **專案 Tab**：PDCA / 簡單清單模板規劃；步驟可「送進待辦」生成 Task
  （`📁` 來源），完成步驟即入帳；專案進度條、完成/封存/復原。
- **LINE 指令**：`專案`（查看進行中專案進度）。
- 導覽調整：底部新增「專案」，「設定」移到右上角齒輪（避免底部過擠）。

## [1.0.0] — 已部署（新功能階段起點）

### Added
- **習慣 Tab**：每日習慣、連續天數（streak）、21 天養成進度、打卡月曆；
  套用《原子習慣》（微小行動、習慣堆疊提示）。習慣會自動產生當日待辦，
  完成即入帳，並在待辦頁顯示 `🔁` 來源標籤。
- **LINE 指令**：`習慣`、`習慣 新增 [名稱]`。
- 基礎建設：`Task.source`（habit / project）。

## [0.0.0] — 已發佈基礎版

- 撲滿（餘額 + 願望進度條）、待辦（記錄、篩選、可折疊）、日記、願望、設定。
- 自訂分類、大/小任務獎勵、中文輸入法友善送出。
- LINE Bot：待辦 / 願望 / 撲滿查詢、`綁定` 配對碼與網頁同步。
- Email / Google 登入 + Supabase 雲端同步（手機與電腦同一份資料）。
- 品牌 LOGO（撲滿圖示，主色 `#00804F`）。
