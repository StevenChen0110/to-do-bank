# Changelog

版本規則：從「習慣 / 專案」等新想法開始的開發為 **1.x**；先前已發佈的基礎版為 **0.x**。
開發流程：本地開發與測試為主，版本管理手動進行，不再自動 commit / push GitHub / 部署。

## [1.3.0] — 本地開發（尚未部署）

### Added
- **優先級拖曳排序**：優先級檢視改為可拖曳看板（`@dnd-kit`，支援手機長按拖曳）。
  在高/中/低區帶內拖曳可調整順序，跨區帶拖曳即改變優先級；以 `order` 欄位持久化。

## [1.2.0] — 本地開發（尚未部署）

### Added
- **待辦「優先級」檢視**：任務新增 `priority`（高/中/低）；待辦頁第三種顯示
  方式「優先級」，依高/中/低分組、未完成在前。
- **快速記錄**：優先級檢視頂部有快速新增列（選優先級 + 輸入即記今日，IME 友善）。
- **一鍵調整**：每張任務卡片有可點的優先級小標籤，點一下循環 高→中→低。
- 工作待辦＝用既有「工作」分類在此檢視篩選（未另開 Tab）。

## [1.1.0] — 本地開發（尚未部署）

### Added
- **專案 Tab**：PDCA / 簡單清單模板規劃；步驟可「送進待辦」生成 Task
  （`📁` 來源），完成步驟即入帳；專案進度條、完成/封存/復原。
- **LINE 指令**：`專案`（查看進行中專案進度）。
- 導覽調整：底部新增「專案」，「設定」移到右上角齒輪（避免底部過擠）。

## [1.0.0] — 已部署（新功能階段起點）

### Added
- **習慣 Tab**：每日習慣、連續天數（streak）、21 天養成進度、打卡月曆；
  套用《原子習慣》（微小行動、習慣堆疊提示）。習慣會自動產生當日待辦，
  完成即入帳，並在待辦頁顯示 `🔁` 來源標籤。
- **LINE 指令**：`習慣`、`習慣 新增 [名稱]`。
- 基礎建設：`Task.source`（habit / project）。

## [0.0.0] — 已發佈基礎版

- 撲滿（餘額 + 願望進度條）、待辦（記錄、篩選、可折疊）、日記、願望、設定。
- 自訂分類、大/小任務獎勵、中文輸入法友善送出。
- LINE Bot：待辦 / 願望 / 撲滿查詢、`綁定` 配對碼與網頁同步。
- Email / Google 登入 + Supabase 雲端同步（手機與電腦同一份資料）。
- 品牌 LOGO（撲滿圖示，主色 `#00804F`）。
